apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pega-test
  namespace: argocd
  labels:
    environment: test
    team: platform
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: pega-platform
  source:
    repoURL: https://github.com/suryayelagam/argocd-kustomize-explore.git
    targetRevision: master
    path: overlays/test
  destination:
    # Local demo: use server URL; Production: use "name: eks-test"
    server: https://kubernetes.default.svc
    namespace: pega-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
