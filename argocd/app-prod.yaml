apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pega-prod
  namespace: argocd
  labels:
    environment: prod
    team: platform
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    notifications.argoproj.io/subscribe.on-sync-succeeded.slack: platform-alerts
spec:
  project: default
  source:
    repoURL: http://gitea.gitea.svc.cluster.local:3000/demo/argocd-kustomize-explore.git
    targetRevision: master
    path: overlays/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: prod
  syncPolicy:
    # PROD: Manual sync only -- no automated sync for safety
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 5m
